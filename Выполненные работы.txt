* НАСТРОЙКА БАЗЫ ДАННЫХ *
1) Создана пустая база данных в MySQL.
2) В файле setting.py указаны настройки подключения к серверу баз данных (ip, логин, пароль, порт, пользователь).
3) В список Installed Apps добавлено название приложения для того, чтобы django включил это приложение.

* СОЗДАНИЕ МОДЕЛЕЙ *
4) В файле model.py созданы модели сущностей для базы данных, указаны названия таблиц
   и возвращаемые строки (чтобы при выводе данных возвращалось наименование, а не id элемента).
5) Проведена миграция моделей в базу данных (перенос сущностей из файла models.py в базу данных).

* СОЗДАНИЕ ШАБЛОНА *
6) Создана новая папка templates с подпапкой main, в которой будут храниться html страницы и шаблоны.
7) Создан основной html-шаблон base_template.html.
8) В шаблоне создана кодовая база для повторяющихся элементов страниц (верхняя панель).
9) В шаблон подключен Bootstrap.
10) В шаблоне определены блоки с подставляемыми из других страниц данными.

* СОЗДАНИЕ ОСНОВНОЙ СТРАНИЦЫ *
11) Создана основная страница сайта index.html.
12) В основной странице index.html описаны элементы интерфейса, которые подставляются в шаблон.

* СОЗДАНИЕ CSS СТИЛЕЙ *
13) Создана папка static (для хранения таблиц стилей, картинок, видео и т.д.), папка проекта main и подпапка css (для описания таблиц стилей).
14) Описана таблица стилей base.css для шаблона base_template.html.
15) В файле шаблона base_template.html подключена поддержка статических файлов.
16) В файле шаблона base_template.html подключена таблица стилей base.css.
17) В файл settings.py добавлена настройка для поддержки статических файлов.

* СОЗДАНИЕ ФАЙЛА ССЫЛОК ДЛЯ ПРИЛОЖЕНИЯ MAIN И НАСТРОЙКА ССЫЛОК ДЛЯ ПРОЕКТА *
18) В приложении main создан новый файл urls.py для описания разделов сайта и выполняемых методов вывода информации на экран.
19) В основном файле urls.py подключена библиотека для обработки ссылок на другие файлы urls.py
    (в случае, если каждый раздел сайта представляет собой отдельное приложение).
20) В основном файле urls.py указана настройка, которая в случае перехода пользователя в корень сайта
    остальные ссылки обрабатываются файлом ссылок main/urls.py.
21) В основном файле urls.py подключена библиотека для работы со статическими файлами.
22) В основном файле urls.py описана стандартная настройка для поддержки статических файлов.

* СОЗДАНИЕ ФУНКЦИИ ВЫВОДА ГЛАВНОЙ СТРАНИЦЫ НА ЭКРАН *
23) В приложении main в файле views.py описан метод, отображающий (рендерящий) страницу main/index.html.
24) В файл main/urls.py подключен файл views.py из той же директории.
25) В файл main/urls.py добавлена настройка, которая при переходе в корень сайта запускает метод index для открытия начальной страницы сайта.
